CREATE TABLE Training (
    id TEXT NOT NULL PRIMARY KEY,
    date TEXT NOT NULL,
    dataPoints TEXT NOT NULL,
    duration INTEGER NOT NULL
);

selectAll:
SELECT * FROM Training;

insert:
INSERT INTO Training(id, date, dataPoints, duration)
VALUES (?, ?, ?, ?);

selectById:
SELECT * FROM Training WHERE id = ?;